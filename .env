# Safe to commit — this is the publishable/anon key, meant for client-side
# use. Row-level security in Postgres is the actual authorization boundary,
# not secrecy of this key. Never put the service_role key here.
EXPO_PUBLIC_SUPABASE_URL=https://jiohhwahvzajvidbiqnm.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_69dScqBVzWVq_SLUJQUKNA_cEhBJfnk

# RevenueCat public SDK key (App Store "public app-specific API key" from
# the RevenueCat dashboard — this is the client-facing key, safe to commit,
# same category as the Supabase key above; never the secret/webhook key).
# Left blank until Jay creates a RevenueCat project — the app runs
# free-tier-only with this unset, it does not crash. See DRILLSTREAK.md
# Monetization section for the setup steps.
EXPO_PUBLIC_REVENUECAT_API_KEY=
